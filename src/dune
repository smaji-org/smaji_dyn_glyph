(include_subdirs unqualified)

(executable
  (name smaji_dyn_glyph)
  (public_name smaji_dyn_glyph)
  (libraries smaji_god core core_unix.command_unix)
  (flags (:standard -safe-string))
  (preprocess (pps ppx_let))
  )

